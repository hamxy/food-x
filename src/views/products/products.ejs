<%- include('../partials/header', {title: title}) %>

    <h1><%= title %></h1>
    
    <ul>
        <% products.forEach(product => { %>
            <li><%= product.name %></li>
        <% }); %>
    </ul>

<%- include('../partials/footer') %>